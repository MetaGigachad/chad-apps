map $host $auth_frontend_host {
    default "$AUTH_FRONTEND_HOST";
}

map $host $auth_api_host {
    default "$AUTH_API_HOST";
}

map $host $auth_oauth2_host {
    default "$AUTH_OAUTH2_HOST";
}

map $host $onboarding_frontend_host {
    default "$ONBOARDING_FRONTEND_HOST";
}

map $host $onboarding_api_host {
    default "$ONBOARDING_API_HOST";
}
