apiVersion: v1
kind: Service
metadata:
  name: onboarding-frontend
spec:
  selector:
    app: onboarding-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: onboarding-frontend
spec:
  selector:
    matchLabels:
      app: onboarding-frontend
  template:
    metadata:
      labels:
        app: onboarding-frontend
    spec:
      containers:
      - name: onboarding-frontend
        image: {{ $.Values.onboarding.frontend.image }}
        ports:
        - containerPort: 80
